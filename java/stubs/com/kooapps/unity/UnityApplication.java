package com.kooapps.unity;

/**
 * Compile-time stub for the game's own Application class.
 *
 * Only exists so javac can resolve the superclass; the generated .class file is
 * deleted before d8 runs, so the real implementation shipped inside the game's
 * classes6.dex is the one that is used at runtime.
 */
public class UnityApplication extends android.app.Application {
}
